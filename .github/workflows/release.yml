# PyPI trusted publisher config will need an update if this file is renamed.
# See: https://docs.pypi.org/trusted-publishers/adding-a-publisher/

name: release
on:
  workflow_dispatch: null
  release:
    types: [ published ]
jobs:
  release:
    runs-on: ubuntu-latest
    environment: pypi-release
    permissions:
      id-token: write

    steps:
      - name: checkout repo
        uses: actions/checkout@v6

      - name: setup python 3
        uses: actions/setup-python@v7
        with:
          python-version: '3.x'

      - name: build the package
        run: make build
        env:
          SPECDOC_VERSION: ${{ github.event.release.tag_name }}

      - name: publish the release to pypi
        uses: pypa/gh-action-pypi-publish@ba38be9e461d3875417946c167d0b5f3d385a247 # pin@v1.14.1
